services:
  - type: web
    name: animedb
    env: node
    buildCommand: bash ./render-build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true 